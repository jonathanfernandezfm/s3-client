# Clear feedback when billing actions fail

The "Manage billing" button now shows an error notification instead of failing
silently when the Stripe billing portal can't be opened.

## What's new

- **Billing error feedback** — if opening the Stripe customer portal fails, you now see a
  clear error message instead of nothing happening.
